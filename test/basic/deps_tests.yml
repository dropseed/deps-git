tests:
- name: Basic test
  repo: repo_current
  diff: repo_updated
  data: dependencies.json
  user_config:
    path: /
    settings:
      remotes:
      - url: https://github.com/getsentry/sentry-javascript.git
        replace_in_files:
        - filename: file.txt
          pattern: raven==(\S+)
          tag_prefix: raven-js@
